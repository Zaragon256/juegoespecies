console.log("loading main.js ...");

console.log(species);

console.log("loading main.js completed");

// random number between 0 and max -1
function getRandomInt(max) {
    return Math.floor(Math.random() * max);
}